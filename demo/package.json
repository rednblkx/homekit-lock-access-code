{
  "name": "hap-acccesscode-example",
  "version": "1.0.0",
  "description": "An example lock accessory with access codes support written using HAPNodeJS as a library.",
  "main": "lib/lock.js",
  "scripts": {
    "clean": "rimraf ./lib",
    "build": "npm run clean && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npm run build && node ./lib/lock.js"
  },
  "author": "White Red <rednblkx@protonmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/rednblkx/homekit-access-codes.git"
  },
  "dependencies": {
    "ber-tlv": "^0.9.2",
    "hap-nodejs": "latest"
  },
  "devDependencies": {
    "@types/node": "10.17.19",
    "rimraf": "^3.0.2",
    "ts-node": "^8.8.2",
    "typescript": "^5.4.3"
  }
}
